# Configuações do ambiente
ENV='development'
TESTING=(True if ENV == 'development' else False)
DEBUG=(True if ENV == 'development' else False)
SECRET_KEY='DONT SHARE WITH ANYONE'

# Configuações de banco de dados
DB_ADDR='db_address'
DB_NAME='db_name'
DB_PASS='db_pass'
DB_PORT='db_port'
DB_USER='db_user'